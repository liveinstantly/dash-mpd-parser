{
  "name": "dash-mpd-parser-package-test-cjs",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "main": "index.ms",
  "scripts": {
    "pkglink": "yarn link @liveinstantly/dash-mpd-parser",
    "start": "node index.js",
    "clean": "rimraf node_modules",
    "test": "yarn && yarn pkglink && yarn start && yarn clean"
  },
  "dependencies": {
    "node-fetch": "2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
